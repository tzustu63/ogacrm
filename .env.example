# 資料庫配置
DB_NAME=recruitment_crm
DB_USER=postgres
DB_PASSWORD=請更改為強密碼

# JWT 配置
JWT_SECRET=請使用 openssl rand -hex 32 生成
JWT_EXPIRES_IN=24h

# 加密金鑰（必須是 64 個十六進制字符）
ENCRYPTION_KEY=請使用 openssl rand -hex 32 生成

# Node.js 環境
NODE_ENV=production

# 端口配置（生產環境通常不需要暴露，由反向代理處理）
# PORT=5000
